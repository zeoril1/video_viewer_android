<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0A0E13"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="64dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Адрес сервера Video Viewer"
        android:textColor="#FFFFFF"
        android:textSize="26sp"
        android:textStyle="bold" />

    <EditText
        android:id="@+id/serverInput"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="24dp"
        android:hint="192.168.1.10:8080"
        android:imeOptions="actionDone"
        android:inputType="textUri"
        android:selectAllOnFocus="true"
        android:textColor="#FFFFFF"
        android:textColorHint="#88FFFFFF"
        android:textSize="22sp" />

    <TextView
        android:id="@+id/settingsHint"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:text="Введите IP/хост и порт вашего сервера. Протокол http:// добавится автоматически."
        android:textColor="#BBBBBB"
        android:textSize="16sp" />

    <Button
        android:id="@+id/btnSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:minWidth="240dp"
        android:text="Сохранить и открыть"
        android:textSize="18sp" />
</LinearLayout>
